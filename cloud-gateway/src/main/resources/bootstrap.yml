spring:
  application:
    name: cloud-gateway
  profiles:
    active: @profileActive@
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        group: CN
        file-extension: yaml
        namespace: start-cloud